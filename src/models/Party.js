export default class Party{
  constructor (id, name, initials, number){
    this.id = id;
    this.name = name;
    this.initials = initials;
    this.number = number;
    this.candidate = [];
  }
  //
}